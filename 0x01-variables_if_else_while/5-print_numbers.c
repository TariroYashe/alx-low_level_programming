#include <stdio.h>
#include <stdlib.h>
#include <string.h>

/**
 * main - entry point
 *
 * Description: program that prints 1 to 9
 *
 * Return: always 0
 */

int main(void)

{
index i;

for (index = 0; index < 10; index++)
{
putchar(index);
}
putchar('\n);
return (0);
}
